<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.snark.walletApp">


    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name=".Activities.TransactionSentActivity"></activity>
        <activity android:name=".Activities.ContactsActivity" />
        <activity android:name=".Activities.RestoreWalletActivity" />
        <activity android:name=".Activities.TxInfoActivity" />
        <activity android:name=".Activities.HistoryActivity" />
        <activity android:name=".Activities.QRScannerActiviry" />
        <activity android:name=".Activities.SendActivity" />
        <activity android:name=".Activities.ReceiveActivity" />
        <activity android:name=".Activities.WelcomeActivity" />
        <activity android:name=".Activities.HomeActivity" />
        <activity android:name=".Activities.Balance" />
        <activity android:name=".Activities.CreateWalletActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>